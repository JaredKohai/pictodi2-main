import 'package:flutter/material.dart';

class PictogramsPage extends StatelessWidget {
  const PictogramsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(); // Puedes modificar esto según lo que necesites para esta página
  }
}
